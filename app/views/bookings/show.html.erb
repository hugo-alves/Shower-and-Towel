<div class="text-center ">
  <h1>Booking </h1>
  <div class="grey-wrapper wrapper text-left ">
    <div class="row">
      <div id="margin-booking" class="col-md-6 col-md-offset-3">
        <p>Name: <%= @booking.user.name%></p>
        <p>Owner: <%= User.find(Bathroom.find(@booking.bathroom_id).user_id).name %></p>
        <p>Address: <%= @booking.bathroom.address %></p>
        <p>Price: <%= @booking.price %>€</p>
        <p>Boook date: <%= @booking.book_time.strftime("%d %m %Y, %Hh%M") %></p>
      </div>
    </div>
  </div>
</div>
